## Publishing workflow

- Make fixes locally and test them first.
- Show the result and ask for approval before pushing.
- After approval, commit and push directly to `main`.
- Do not create a branch or pull request unless explicitly requested.
- Never include unrelated changes.
